window.addEventListener('scroll', function () { 
    const header = document.querySelector('.navbar__top');
    header.classList.toggle("sticky", window.scrollY > 58);
})

// Nút play

// Thêm nút Play nha chưa biết làm

window.addEventListener('scroll', function () { 
    const header = document.querySelector('.navbar__top');
    header.classList.toggle("sticky", window.scrollY > 58);
})